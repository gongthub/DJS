namespace DJS.Core.CPlatform.Server.Utilities
{
    public enum RemoteInvokeType
    {
        Heartbeat,
        TriggerJob,
        SubScriptionExecute,
        SubScriptionScheduler,
        SetExecuteJob
    }
}
